# Sky Control telemetry debug

Copy the `internal` folder into your existing skycontrol folder, replacing:

- internal/telemetry/tacview.go
- internal/app/app.go

Then run BUILD.bat.

This only adds console printouts of what Tacview is sending. It does not change ATC behavior.
